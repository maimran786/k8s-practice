apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    app: postgres-db
spec:
  type: ClusterIP
  selector:
    app: postgres-db
  ports:
   - name: "db"
     port: 5432
     targetPort: 5432
